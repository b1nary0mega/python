This is a directory containing files that are generated from reading "Black Hat Python"

The readme file (this file), doesn't contain anything other than my ramblings and a way to test the git commit engine.
